import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "npm:@supabase/supabase-js@2.57.0";
const corsHeaders={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST, OPTIONS"};const json=(data:unknown,status=200)=>Response.json(data,{status,headers:corsHeaders});function getSecretKey(){const modern=Deno.env.get("SUPABASE_SECRET_KEYS");if(modern)try{const p=JSON.parse(modern);if(p.default)return p.default as string;}catch{}return Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")??"";}
Deno.serve(async(req:Request)=>{if(req.method==="OPTIONS")return new Response("ok",{headers:corsHeaders});if(req.method!=="POST")return json({error:"Method not allowed"},405);const token=(req.headers.get("Authorization")??"").replace(/^Bearer\s+/i,"");if(!token)return json({error:"Unauthorized"},401);const url=Deno.env.get("SUPABASE_URL")??"";const key=getSecretKey();if(!url||!key)return json({error:"Server configuration error"},500);const admin=createClient(url,key,{auth:{persistSession:false}});const{data:userData,error:userError}=await admin.auth.getUser(token);const user=userData.user;if(userError||!user)return json({error:"Unauthorized"},401);const{error:maintenanceError}=await admin.rpc("server_assert_app_active",{p_player_id:user.id});if(maintenanceError){const message=maintenanceError.message??"APP_MAINTENANCE";return json({error:message},message.includes("APP_MAINTENANCE")?503:500);}const body=await req.json().catch(()=>({}));try{if(body.action==="favorite"){if(!body.cardId||typeof body.favorite!=="boolean")return json({error:"cardId and favorite are required"},400);const{error}=await admin.rpc("server_set_favorite",{p_player_id:user.id,p_card_id:body.cardId,p_favorite:body.favorite});if(error)throw error;return json({data:{favorite:body.favorite}});}if(body.action==="friend"){if(!body.targetId||!body.friendAction)return json({error:"targetId and friendAction are required"},400);const{data,error}=await admin.rpc("server_friend_action",{p_actor_id:user.id,p_target_id:body.targetId,p_action:body.friendAction});if(error)throw error;return json({data:{status:data}});}if(body.action==="daily"){const{data,error}=await admin.rpc("server_claim_daily_reward",{p_player_id:user.id});if(error)throw error;return json({data});}if(body.action==="claim_mission"){if(!body.missionId)return json({error:"missionId is required"},400);const{data,error}=await admin.rpc("server_claim_daily_mission",{p_player_id:user.id,p_mission_id:body.missionId});if(error)throw error;return json({data});}if(body.action==="change_username"){if(typeof body.username!=="string")return json({error:"username is required"},400);const{data,error}=await admin.rpc("server_change_username",{p_player_id:user.id,p_username:body.username});if(error)throw error;return json({data:{username:data}});}if(body.action==="refresh_achievements"){const{error}=await admin.rpc("server_refresh_player_achievements",{p_player_id:user.id});if(error)throw error;return json({data:{refreshed:true}});}if(body.action==="equip_title"){if(body.achievementId!==null&&typeof body.achievementId!=="string")return json({error:"achievementId is required"},400);const{data,error}=await admin.rpc("server_set_equipped_title",{p_player_id:user.id,p_achievement_id:body.achievementId??null});if(error)throw error;return json({data:{achievementId:data}});}if(body.action==="set_rating_visibility"){if(typeof body.visible!=="boolean")return json({error:"visible is required"},400);const{data,error}=await admin.rpc("server_set_rating_visibility",{p_player_id:user.id,p_visible:body.visible});if(error)throw error;return json({data:{visible:data}});}return json({error:"Invalid action"},400);}catch(error){const message=error instanceof Error?error.message:String(error);const status=message.includes("FORBIDDEN")?403:message.includes("NOT_FOUND")||message.includes("NOT_OWNED")?404:409;return json({error:message},status);}});
