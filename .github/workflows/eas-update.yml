name: Publish Production Update

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - app/**
      - src/**
      - assets/**
      - public/**
      - app.json
      - package.json
      - package-lock.json
      - scripts/generate-trainer-assets.py
      - scripts/project-audit.mjs
      - scripts/regression-audit.mjs
      - .github/workflows/eas-update.yml

permissions:
  contents: read

concurrency:
  group: production-deploy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Check Expo token
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: |
          if [ -z "$EXPO_TOKEN" ]; then
            echo "EXPO_TOKEN is not configured in this repository."
            exit 1
          fi

      - name: Set up EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          packager: npm
          token: ${{ secrets.EXPO_TOKEN }}

      - name: Install dependencies
        run: npm ci

      - name: Prepare Trainer Collection artwork
        run: |
          python -m pip install --disable-pip-version-check pillow
          python scripts/generate-trainer-assets.py

      - name: Verify OTA/native integrity
        run: npm run verify

      - name: Load Expo preview environment
        run: eas env:pull --environment preview --path .env.local --non-interactive

      - name: Export web app
        run: npx expo export --platform web

      - name: Deploy web app to production
        run: eas deploy --prod --environment preview --dev-domain pokemon-cards-frontwork --non-interactive

      - name: Publish Android update to production
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
        run: eas update --channel production --platform android --environment production --message "GitHub ${GITHUB_SHA:0:7}" --non-interactive
